{
  "name": "Devil's Telegram Broadcast & Auto Reaction Bot",
  "description": "A Telegram bot that can broadcast messages to all joined groups/channels, auto-react to messages, manage admins, and show stats. Built with Pyrogram & MongoDB.",
  "repository": "https://github.com/YourUsername/YourRepoName",
  "keywords": ["telegram", "bot", "broadcast", "pyrogram", "mongodb", "auto-reaction"],
  "env": {
    "API_ID": {
      "description": "Your Telegram API ID from my.telegram.org",
      "required": true
    },
    "API_HASH": {
      "description": "Your Telegram API HASH from my.telegram.org",
      "required": true
    },
    "BOT_TOKEN": {
      "description": "Bot token from @BotFather",
      "required": true
    },
    "MONGO_URI": {
      "description": "Your MongoDB connection string",
      "required": true
    },
    "OWNER_IDS": {
      "description": "Space-separated list of Telegram user IDs who will be bot owners",
      "required": true
    }
  },
  "stack": "heroku-24"
}
